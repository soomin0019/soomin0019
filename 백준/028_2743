#include <stdio.h>
#include <string.h>

int main(){

	char N[105] = {};
	int n=0;

	scanf("%s", N);
	for (int i = 0; ; i++) {
		if (N[i] == '\0')
			break;
	}
	n = strlen(N);
	printf("%d", n);
	
	return 0;
}
