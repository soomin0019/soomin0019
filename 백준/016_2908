#include<stdio.h>

int main() {
	int A, B;
	scanf("%d %d", &A, &B);
	
	A = A % 10 * 100 + ((A % 100) / 10) * 10 + A / 100;
	B = B % 10 * 100 + ((B % 100) / 10) * 10 + B / 100;

	if (A > B) printf("%d", A);
	else printf("%d", B);

	return 0;
}

int reverse(int A, int B) {
	

	return A, B;
}
